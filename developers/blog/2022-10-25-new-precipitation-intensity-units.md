从 Home Assistant Core 2022.11 开始，以下常量（用于 volumetric flux，如降水强度）已弃用，由 `UnitOfVolumetricFlux` 枚举替代：

* `PRECIPITATION_INCHES_PER_HOUR` 由 `UnitOfVolumetricFlux.INCHES_PER_HOUR` 替代
* `PRECIPITATION_MILLIMETERS_PER_HOUR` 由 `UnitOfVolumetricFlux.MILLIMETERS_PER_HOUR` 替代
* `SPEED_INCHES_PER_DAY` 由 `UnitOfVolumetricFlux.INCHES_PER_DAY` 替代
* `SPEED_INCHES_PER_HOUR` 由 `UnitOfVolumetricFlux.INCHES_PER_HOUR` 替代
* `SPEED_MILLIMETERS_PER_DAY` 由 `UnitOfVolumetricFlux.MILLIMETERS_PER_DAY` 替代
