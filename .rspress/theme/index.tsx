// .rspress/theme/index.tsx
import type { Theme } from 'rspress/theme';
import DefaultTheme from 'rspress/theme-default';
import './global.css';

const theme: Theme = {
  ...DefaultTheme,
};

export default theme;
