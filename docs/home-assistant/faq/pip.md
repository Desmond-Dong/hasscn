---
title: 没有名为 pip 的模块
description: 没有名为 pip 的模块
---

[`pip`](https://pip.pypa.io/en/stable/) 通常会随较新的 Python 3 一起提供，但某些发行版会省略它。如果您无法运行 `python3 -m pip --version`，可以下载[安装脚本](https://bootstrap.pypa.io/get-pip.py)，再使用 Python 3 运行它来安装 `pip`：

```bash
python3 get-pip.py
```
